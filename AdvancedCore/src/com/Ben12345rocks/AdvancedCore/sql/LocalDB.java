package com.Ben12345rocks.AdvancedCore.sql;

import org.bukkit.plugin.java.JavaPlugin;

//import java.util.Arrays;
//import java.util.List;

public class LocalDB extends JavaPlugin {

	// public void onEnable() {
	//
	// Column ign = new Column("ign", DataType.STRING, 0);
	// Column name = new Column("name", DataType.STRING, 0);
	// Column age = new Column("age", DataType.INTEGER, 0);
	// Table table = new Table("theTable", Arrays.asList(ign, age, name));
	//
	// Database database = new Database(this, "theDatabase", table,
	// "Data/PlayerData");
	//
	// ign.setValue("ZP4RKER");
	// name.setValue("Yaya");
	// age.setValue(16);
	//
	// table.insert(Arrays.asList(ign, age, name));
	//
	// List<Column> result = table.getExact(ign);
	//
	// getLogger().info("You are " + result.get(1).getValue() + " years old!");
	//
	// ign.setValue("TheDagga");
	//
	// table.insert(Arrays.asList(ign, age, name));
	//
	// List<List<Column>> results = table.search(name);
	//
	// getLogger().info("Both names are " + results.get(0).get(2).getValue());
	//
	// name.setValue("Yaya Toure");
	//
	// table.update(ign, Arrays.asList(name));
	//
	// results = table.search(age);
	//
	// getLogger().info("New name is " + results.get(1).get(2).getValue());
	//
	// Column newIGN = new Column("ign", DataType.STRING, 0);
	// newIGN.setValue("TheDaga");
	//
	// table.update(ign, Arrays.asList(newIGN));
	//
	// Column streak = new Column("streak", DataType.INTEGER, 0);
	// Column total = new Column("total", DataType.INTEGER, 0);
	//
	// ign.setValue("ZP4RKER");
	// streak.setValue(9);
	// total.setValue(19);
	//
	// Table killsTable = new Table("Kills", Arrays.asList(ign, streak, total));
	// database.addTable(killsTable);
	// killsTable.insert(Arrays.asList(ign, streak, total));
	//
	// getLogger().info("You have a streak of " +
	// killsTable.getExact(ign).get(1).getValue() + "!");
	//
	// }

}